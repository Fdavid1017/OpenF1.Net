## Language

- All repository content is written in **English**: READMEs and other markdown docs, code
  comments, XML doc comments, commit messages, and identifiers.
- Chat replies to the user are written in **Hungarian**.

## Agent skills

### Issue tracker

Issues live as markdown files under `.scratch/<feature-slug>/`. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
